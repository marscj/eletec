from django.conf.urls import url

# from .views import GenerateOTP, ValidateOTP

urlpatterns = [
    # url(r'^auth/email/generate/$', GenerateOTP.as_view(), name="email_generate"),
    # url(r'^auth/email/validate/$', ValidateOTP.as_view(), name="email_validate"),
]